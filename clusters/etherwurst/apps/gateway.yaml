apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: etherwurst-gateway
  namespace: ethereum
spec:
  gatewayClassName: nginx
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: checkmyethereum.com
      allowedRoutes:
        namespaces:
          from: Same
    - name: https
      port: 443
      protocol: HTTPS
      hostname: checkmyethereum.com
      tls:
        mode: Terminate
        certificateRefs:
          - kind: Secret
            name: etherwurst-tls
      allowedRoutes:
        namespaces:
          from: Same
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: spotty-gateway
  namespace: ethereum
spec:
  gatewayClassName: nginx
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      hostname: azurespotty.com
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      port: 443
      protocol: HTTPS
      hostname: azurespotty.com
      tls:
        mode: Terminate
        certificateRefs:
          - kind: Secret
            name: azurespotty-tls
      allowedRoutes:
        namespaces:
          from: All
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-to-https-redirect
  namespace: ethereum
spec:
  hostnames:
    - checkmyethereum.com
  parentRefs:
    - name: etherwurst-gateway
      sectionName: http
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hazmebeenscammed-web
  namespace: ethereum
spec:
  hostnames:
    - checkmyethereum.com
  parentRefs:
    - name: etherwurst-gateway
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: hazmebeenscammed-web
          port: 80
---
# ── azurespotty.com: HTTP → HTTPS redirect ──────────────────────────────────
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: spotty-http-redirect
  namespace: spot-dashboard
spec:
  hostnames:
    - azurespotty.com
  parentRefs:
    - name: spotty-gateway
      namespace: ethereum
      sectionName: http
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
---
# ── azurespotty.com: HTTPS → spot-dashboard service ─────────────────────────
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: spotty-https
  namespace: spot-dashboard
spec:
  hostnames:
    - azurespotty.com
  parentRefs:
    - name: spotty-gateway
      namespace: ethereum
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: spot-dashboard
          port: 80
